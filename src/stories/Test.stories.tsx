import React from 'react';
import  TestTheme from '../component/Test';

export default {
    title: 'Components/Test/Theme'
}

const Template = (args) => <TestTheme {...args} />

export const Test = Template.bind({});

